from setuptools import setup

setup(name='campus_gym',  # name of the package
      version='0.0.1',  # version of this release
      install_requires=["gymnasium==0.26.0"]  # specifies the minimal list of libraries required to run the package correctly
      )
