wandb:
#  project: "approximated-sir"
  project: "campus-indoor-model"
  entity: "safe-campus"

directories:
  results_directory: "results"
  config_directory: "config"

environment:
  environment_id: 'CampusGymEnv-v0'
  seed: 100

alpha: 0.9  # Example alpha value, change as needed
